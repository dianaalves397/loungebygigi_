import { requireAdmin } from "@/lib/auth"; import { getSettings } from "@/lib/settings";
export const dynamic="force-dynamic"; export const revalidate=0; export async function POST(){const u=await requireAdmin(); if(u) return u; try{const s=await getSettings(); const mode=s.payments?.paypalMode||"sandbox"; const base=mode==="live"?"https://api-m.paypal.com":"https://api-m.sandbox.paypal.com"; const id=s.payments?.paypalClientId, secret=s.payments?.paypalClientSecret; if(!id||!secret) throw new Error("PayPal Client ID ou Client Secret em falta."); const res=await fetch(`${base}/v1/oauth2/token`,{method:"POST",headers:{Authorization:`Basic ${Buffer.from(`${id}:${secret}`).toString("base64")}`,"Content-Type":"application/x-www-form-urlencoded"},body:"grant_type=client_credentials",cache:"no-store"}); const data=await res.json(); if(!res.ok) throw new Error(data.error_description||data.error||"Erro PayPal"); return Response.json({message:`PayPal OK em modo ${mode}.`});}catch(e:any){return Response.json({error:e.message||"Erro PayPal"},{status:500});}}

